# Build para Vercel (modo demo estático, sin backend)
# Sube estas variables en el dashboard de Vercel:
# Project → Settings → Environment Variables

VITE_BASE_PATH=/
VITE_DEMO_MODE=true
VITE_USE_HASH_ROUTER=false
VITE_API_URL=/api/v1
